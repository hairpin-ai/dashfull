import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'hairpin',
};

export default function HomePage() {
  return <HomeView />;
}
